.status-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 100%;
}

.banner {
  margin-top: 30px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.icon {
  width: 180px;
  height: @width;
}
