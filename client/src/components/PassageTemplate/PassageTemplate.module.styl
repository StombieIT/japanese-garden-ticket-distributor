.passage-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.passage {
  min-width: 900px;
}

.submit-button {
  font-size: 20px;
  margin-top: 40px;
}
